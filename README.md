# vision-bridge-skills
Route images to vision-capable models when your primary chat model cannot view images.  Supports any Anthropic Messages API-compatible multimodal models. Two-stage workflow:  vision model analyzes → primary model maps to actions.
